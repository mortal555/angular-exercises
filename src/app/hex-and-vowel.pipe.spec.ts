import { HexAndVowelPipe } from './hex-and-vowel.pipe';

describe('HexAndVowelPipe', () => {
  it('create an instance', () => {
    const pipe = new HexAndVowelPipe();
    expect(pipe).toBeTruthy();
  });
});
